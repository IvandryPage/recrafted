#include "../include/Dialogue.h"

Dialogue::Dialogue(int character_index_param, std::string line_param, std::string monologue_param)
{
    character_index = character_index_param;
    line = line_param;
    monologue = monologue_param;
}